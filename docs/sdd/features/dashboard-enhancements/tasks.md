# Tasks: Dashboard Enhancements

- [ ] **Task 1:** Update `LocaleContext` (or `pt-BR.json` / `en.json`) with new translation keys for charts and toggle.
- [ ] **Task 2:** Update `DashboardChart` to include a chevron toggle for the table, reading/writing to `localStorage`.
- [ ] **Task 3:** Extend `useDashboardViewModel` to aggregate transactions into `expensesByCategory` and provide it to the view.
- [ ] **Task 4:** Create `ExpensesByCategoryChart` component using `recharts` PieChart.
- [ ] **Task 5:** Integrate the new charts into `Dashboard.tsx` below `DashboardChart`.
- [ ] **Task 6:** Apply premium CSS styles to the new components (glass-panel, animations, hover effects).
- [ ] **Task 7:** Run tests and ensure no regressions.
